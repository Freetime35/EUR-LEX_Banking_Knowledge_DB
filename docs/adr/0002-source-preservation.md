# ADR 0002 — Conservation des sources

## Statut
Accepté.

## Décision
Les fichiers officiels HTML, PDF, XML/Formex, RDF et les réponses de métadonnées sont conservés sans modification dans `data/raw`.

Les transformations sont écrites dans `data/processed`. Les livrables utilisateur sont écrits dans `data/exports`.
