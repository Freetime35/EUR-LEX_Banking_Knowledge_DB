# Contribuer

1. Créer une branche `feature/...`.
2. Ajouter ou mettre à jour les tests.
3. Lancer `ruff check .`, `mypy src/ekb` et `pytest`.
4. Documenter les décisions d'architecture importantes dans `docs/adr`.
